{
  "builds": [{ "src": "api/index.py", "use": "@vercel/python" }],
  "routes": [{ "src": "/(.*)", "dest": "api/index.py" }],
  "regions": ["iad1"],
  "functions": {
    "api/index.py": {
      "maxDuration": 300
    }
  }
}
